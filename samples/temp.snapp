class ExampleClass {

    int counter = 0;

    new () {
        io.print("Hello World");
        io.print("Constructor");
    }

    int incrementCount() {
        counter = counter + 1; // ++, --, +=, etc. not supported yet
        return counter;
    }

}

str foo = "Hello World";

ExampleClass example = ExampleClass();

io.print(example.incrementCount());

io.print(string.length("Hello World"));

io.print(example.incrementCount());
